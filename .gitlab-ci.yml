stages:
  - ci-image
  - deploy

ci-image:default:
  stage: ci-image
  # when: manual
  only: ["master", "pawel/ci_cd"]
  tags: ["runner:docker", "size:large"]
  image: $BUILDENV_REGISTRY/docker:18.03.1
  script:
    - docker ps
    # - image: $BUILDENV_REGISTRY/docker:18.03.1
    # - wget -C "https://github.com/bazelbuild/bazel/releases/download/2.1.0/bazel-2.1.0-installer-linux-x86_64.sh"
